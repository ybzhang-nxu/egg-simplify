# ESymb 下一阶段（C）：单圈 Hankel 子块（prefix/suffix）rank
**用单圈海量 terms 弥补 loops 太短：更接近自动机/表示论的“状态合并证据”**

> **定位**：C 线把 Hankel 的“第二维”从 loop 轴换成 **word 的 prefix/suffix**。  
> 你不再依赖跨圈长度（只有 6 点），而是在固定 L 的单圈里利用海量 terms，做一个小矩阵（例如 81×81）的秩与行依赖分析，从而得到“状态空间维数小/可合并”的证据。

---

## 1. 背景与理论动机

### 1.1 经典 Hankel 观点：Hankel 秩 = 最小线性表示维数
对加权语言 \(f:\Sigma^\*\to\mathbb{Q}\) 可定义无限 Hankel 矩阵：
\[
H[u,v] := f(uv).
\]

在加权自动机/线性表示理论中，**Hankel 矩阵的秩**等价于最小线性表示（最小状态空间）维数：秩越小，表示越低维，越像“小自动机”。

> C 线并不要求你真的重建自动机；你只要利用“低秩 ⇒ 存在线性依赖/状态可合并”这一可验证结论。

### 1.2 为什么 C 能绕开 loops=6 的限制
跨圈信息短，但单圈 terms 极多（L6 甚至达到百万量级）。  
C 线把信息来源换成“单圈内 prefix→suffix 的统计结构”，因此更稳、更接近结构本身，而不是被短序列证据卡死。

---

## 2. 定义：单圈内的 prefix/suffix Hankel 子块

设固定某个 loop L（word 长度 n=2L），选定：
- 前缀集合 \(P\subseteq\Sigma^r\)
- 后缀集合 \(S\subseteq\Sigma^k\)

定义子块矩阵（两种版本）：

### C1) 无中间边缘化（要求 r+k=n）
若 \(r+k=2L\)，可以定义：
\[
H^{(L)}_{P,S}[u,v] := f_L(uv),\quad u\in P,\ v\in S.
\]
这更“尖锐”，但只能在 r+k 刚好填满长度时使用。

### C2) 带中间边缘化（推荐：适用于任意 r,k 且 r+k≤n）
更一般（也是你文档里强调的版本）：
\[
H^{(L)}_{r,k}[u,v] := \sum_{m\in\Sigma^{2L-r-k}} f_L(u\,m\,v).
\]
这实际上与 A3 的矩阵一致，但 C 的重点是：**在固定 L 内研究该矩阵的 rank 与行/列依赖**。

---

## 3. 你能从 rank(H) 得到什么结构信息？

### 3.1 低秩意味着“少状态传递”
若存在某个低维线性表示/加权自动机模型，则（在理想情况下）：
\[
\mathrm{rank}\big(H^{(L)}_{r,k}\big) \le d,
\]
其中 d 可解释为“有效状态空间维数”。

即使你不相信模型完全成立，低秩仍然是强的经验性证据：prefix 与 suffix 的耦合通过很少的自由度实现。

### 3.2 行依赖 = 状态合并（可解释的依赖关系）
若存在行依赖：
\[
H[u,\cdot] = \sum_i \beta_i\,H[u_i,\cdot],
\]
则对所有 \(v\in S\) 有：
\[
H[u,v] = \sum_i \beta_i\,H[u_i,v].
\]

解释：
- “以 u 开头”的后缀响应可以由其他前缀的响应线性组合得到
- 在自动机语言里就是：u 对应的状态可被这组状态张成（可合并/可压缩）

### 3.3 为什么这更贴近表示论/谱学习叙事
谱学习里常用 “前缀特征 × 后缀特征” 构造 Hankel/特征矩阵，再用低秩分解估计状态数。  
C 线正是在 ESymb 上做这种“截断 Hankel”实验：不需要重建，只要证明“确实低秩/确实有行依赖”。

---

## 4. 算法（不写代码的“实现级”步骤）

### 4.1 选择 P,S（避免爆炸的两种策略）
1) **短词全枚举**：\(P=\Sigma^r\), \(S=\Sigma^k\)，r,k 取 1 或 2。  
   - 9 字母时 r=k=2 → 81×81（很小）
2) **数据驱动出现集**：只保留在该圈数据中真实出现过的前/后缀，减少空行空列。

### 4.2 单圈 streaming 构造矩阵
对固定 L 流式读取 JSONL term：
- word 长度 n=2L
- 取 prefix u = word[0..r)，suffix v = word[n-k..n)
- 累加：`H[u,v] += coeff`（这是 C2 的边缘化版本；因为中间 m 被自动求和）

注意：你无需存 “word→coeff” 全表，只需存一个 \( |P|\times|S| \) 的小矩阵（稀疏或密集都可）。

### 4.3 rank 与依赖求解（两阶段）
1) **mod p screening**：在多个素数模下计算 rank(H)，得到快速且稳健的 rank 指纹  
2) **exact 验证/依赖抽取**：在有理数域做消元
   - 产出：rank、基、以及若干行依赖关系（优先小支持/小系数）

---

## 5. 输出与解释模板（建议）

### 5.1 统计输出（每个 L, r, k 一行）
- 行/列大小（含去空后的有效大小）
- rank(H)（mod p 聚合 + exact）
- 稀疏度指标：非零项数量、平均每行非零等
- “聚类”摘要：完全相同行、成比例行的数量（很常见且很有解释价值）

### 5.2 依赖关系示例（可读性优先）
给出若干条（按简洁度排序）：
- 支持=2：`row(u) == row(u')` 或 `row(u) == -row(u')`
- 支持=3：`row(u) == row(u1) ± row(u2)`
并附：
- 适用范围：固定 L、固定 r,k、对所有 \(v\in S\) 成立
- 残差验证：exact 为 0

---

## 6. 测试计划（C 线必须做的正确性与稳健性检查）

### 6.1 与 A3 的一致性（强一致性检查）
如果你也计算了同一 (r,k) 的跨圈 A3 矩阵序列 \(M_L\)，那么对固定 L：
- C 构造的 \(H^{(L)}_{r,k}\) 应与 \(M_L\) 的该 L 切片完全一致（同 key 集合/同累加规则）。

### 6.2 行/列边缘化守恒
对固定 L：
\[
\sum_v H[u,v] = c_L^{(u)}\quad(\text{prefix marginal}),\qquad
\sum_u H[u,v] = c_L^{(v)}\quad(\text{suffix marginal}).
\]
这是定义后果，失败即实现错误。

### 6.3 mod p 与 exact 的一致性
- exact rank 应 ≥ 任意模 p rank（一般来说模 p 可能发生额外降秩，但不会“升秩”）
- 若某个 prime 导致 rank 异常低，保守聚合与 exact 应避免误导。

### 6.4 决定性（determinism）
- 同一输入多次运行：rank、依赖集合（按规范化排序）保持一致。

---

## 7. 第一轮实验建议（参数与验收）

### 7.1 建议先固定 L=6 做“高信噪比”探索
- L 越大，terms 越多，矩阵估计越稳定。
- 先做 r=k=1（9×9），再做 r=k=2（81×81）。

### 7.2 验收标准（第一轮）
- 正确性：边缘化守恒（6.2）通过
- 结构性：`rank(H)` 显著小于 min(|P|,|S|)（例如 81×81 时 rank 远小于 81）
- 可解释性：抽取到若干条小支持的行依赖（或大量完全相同行/比例行），作为“状态可合并”证据

---

## 8. 与现有工具链的衔接（不涉及实现细节）
项目强调确定性实验输出与报告契约；README 给出 `mpl-experiments` 是实验 runner，并列举了 ESymb 相关扫描子命令与输出路径的范式，这种“统计 CSV + summary MD”的模式可以直接沿用到 C 线。fileciteturn0file0L141-L146
