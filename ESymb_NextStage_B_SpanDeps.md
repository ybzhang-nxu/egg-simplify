# ESymb 下一阶段（B）：span rank / nullspace
**loops 很少时最有效的“依赖关系挖掘器”**

> **定位**：B 线绕开“单序列只有 6 点难以证实高阶递推”的瓶颈：不再问“每条序列是否低 Hankel 秩”，而是问“很多 observable 在 \(\mathbb{Q}^6\) 里张成的空间有多大、有哪些线性约束”。  
> 产出物是 **跨圈同时成立的线性依赖关系**（可解释的守恒/对称/等价类线索）。

---

## 1. 背景与理论动机

### 1.1 为什么 B 在 loops=1..6 反而更强
- 单序列 Hankel rank 的“可证书区”只覆盖 rank ≤ 2（因为只有 6 点）。
- 但 B 线不需要长时间序列：它是把每个 observable 看成一个 6 维向量做线性代数。
- 对 \(m\) 个 observable，组成矩阵 \(V\in\mathbb{Q}^{m\times 6}\)。因为列数只有 6，**rank(V) ≤ 6**，所以当 \(m\) 稍大时，必然存在大量依赖关系；关键是筛出“像规律”的那一小部分。

### 1.2 “规律”与“巧合”的分界：稀疏 + 小系数
纯 nullspace 往往给出稠密的大系数关系，很难解释，也很可能是“在 6 点上的偶然拟合”。  
更可信、可解释的关系通常满足：
- **小支持（sparse support）**：只涉及 2~4 个桶
- **小系数（small coefficients）**：系数为 \(\{-2,-1,0,1,2\}\) 或小分母有理数

这是把“可解释性”变成了可计算的筛选准则。

---

## 2. 输入对象（observable 家族）

B 线不规定 observable 的来源：你可以把 A 线生成的桶（suffix/prefix/(u,v)）直接喂给 B，或者加入其它非重复 observable。

每个 observable \(i\) 对应跨圈向量：
\[
v_i := \big(c^{(i)}_1, c^{(i)}_2, \dots, c^{(i)}_6\big)\in\mathbb{Q}^6.
\]

组成矩阵（行堆叠）：
\[
V = \begin{pmatrix}
v_1^\top\\
v_2^\top\\
\vdots\\
v_m^\top
\end{pmatrix}\in\mathbb{Q}^{m\times 6}.
\]

---

## 3. 核心任务与算法

### 3.1 任务 1：span rank（自由度）
计算 \(\mathrm{rank}(V)\)，回答：
- “这些 observables 在 6 个 loop 上的变化模式有多少独立方向？”
- 若 rank 远小于 6，说明存在强的跨圈约束或共同驱动模态。

**实践意义**：rank(V) 本身就是一个“复杂度指标”，比单条序列的 rank curve 更稳。

### 3.2 任务 2：nullspace（跨圈恒等式）
求 \(V^\top\) 的零空间：
\[
\alpha\in\mathrm{null}(V^\top)\quad\Longleftrightarrow\quad \sum_{i=1}^m \alpha_i v_i = 0.
\]

展开就是对所有 \(L=1..6\) 成立：
\[
\sum_{i=1}^m \alpha_i c^{(i)}_L = 0.
\]

这正是你要的“跨圈同时成立”的依赖关系。

---

## 4. 让依赖关系“可读”的筛选策略（从易到难）

### 4.1 Pairwise：两桶相等/成比例（支持=2）
直接枚举对 \((i,j)\)：
- 检测 \(v_i = v_j\)（完全相等）
- 检测 \(v_i = -v_j\)
- （可选）检测 \(v_i = q v_j\)，其中 \(q\) 来自一小撮候选有理数（例如 \(\pm 2,\pm 1/2,\pm 3,\pm 1/3\)）

**为什么重要**：这是最强、最易解释的对称性证据——经常对应字母重标记/桶划分等价。

### 4.2 3-term：三项和差（支持=3）
检查：
- \(v_i \pm v_j = v_k\)
- \(v_i = v_j \pm v_k\)

系数限制在 \(\{-1,0,1\}\) 内，优先发现“守恒/分裂/合并”式关系。

### 4.3 4-term：小系数稀疏组合（支持=4）
检查：
\[
\pm v_{i_1} \pm v_{i_2} \pm v_{i_3} \pm v_{i_4} = 0
\]
或允许少量系数为 2。

这一步会显著增加候选数量，因此通常要加约束：
- 先做 hash/签名（例如按模 p 的向量签名）加速筛选
- 再做有理数精确验证

### 4.4 一般 nullspace + 稀疏化（可选进阶）
一般方法求出 nullspace 基后，做“稀疏化”/“短向量”搜索（类似 LLL/整数关系检测的精神，但你可以先用更简单的启发式）：
- 最小化支持大小
- 最小化 \(\ell_1\) 或 \(\ell_\infty\) 系数范数
- 优先小分母

---

## 5. 产出格式（建议）

### 5.1 span 指标
- `rank(V)`、以及按 observable 子集（suffix-only / prefix-only / uv）分组的 rank
- 行/列数、TRIVIAL 数量（全 0 向量）、重复向量数量

### 5.2 依赖关系列表（按“可解释性”排序）
每条关系记录：
- 支持集（涉及哪些桶）
- 系数向量（尽量是小整数/小分母）
- 对 6 个 loop 的残差（应为全 0）
- 可选：给出“人类可读”形式（例如 `bucket[a] == bucket[b]`）

---

## 6. 测试计划（B 线必须保证的可靠性）

### 6.1 精确性与数值健壮
- 所有报告的关系必须在 **有理数域精确成立**（不能只在模 p 下成立）。
- 如果用模 p 做候选筛选，必须做最后的 exact check。

### 6.2 不变性测试（对输入顺序不敏感）
- 打乱桶顺序、打乱 JSONL 读取顺序，输出的“关系集合”（按某种规范化排序）应一致。

### 6.3 退化/边界情况
- 大量 TRIVIAL（全 0）桶：应自动跳过或单独统计，避免产生无意义关系。
- 大量重复向量：pairwise 检测应能快速识别并压缩等价类。

### 6.4 “假阳性”控制
- 限制支持规模与系数范围，并把搜索空间与命中率记录到报告：例如 “尝试了多少 3-term 候选，命中多少”。

---

## 7. 第一轮实验建议（参数与验收）

### 7.1 建议输入集合
优先用 A 线的桶序列作为 v_i：
- suffix k=1（9 个向量）与 k=2（81 个向量）先跑
- prefix r=1/2 类似
- uv (r,k)=(2,2)（最多 6561 个向量）可以在第二阶段跑（先做 TRIVIAL/重复压缩）

### 7.2 验收标准（第一轮）
- 产出可解释的关系：
  - 至少出现若干条支持=2 的相等/相反关系，或支持=3 的和差关系（若存在）
  - 若没有，也要在报告中明确说明“在支持≤3、系数∈{-1,0,1} 的搜索范围内未发现非平凡关系”
- 统计指标稳定：rank(V) 对输入顺序、prime 集合、桶排序不敏感

---

## 8. 与现有工具链的衔接（不涉及实现细节）
你已有的 `mpl-experiments` 体系强调**确定性输出**与实验报告产物（CSV/MD）。README 也说明 `mpl-experiments` 中已有 ESymb 相关扫描工具与固定输出目录约定，可沿用同样的“报告契约”来组织 B 线输出。fileciteturn0file0L141-L146
